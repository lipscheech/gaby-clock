export enum TipoTime {
	HOURS = 'hours',
	MINUTES = 'minutes',
	SECONDS = 'seconds'
}

export enum TipoTimer {
	HOUR_MINUTES_SECONDS = 'hms',
	HOUR_MINUTES = 'hm',
	MINUTES_SECONDS = 'ms',
	MINUTES = 'm',
	SECONDS = 's'
}
